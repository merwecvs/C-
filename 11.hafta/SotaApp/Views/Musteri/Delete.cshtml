@model SotaApp.Data.Musteri
<p>
    <b>
        @Model.MusteriAdi</b> adlı ürünü silmek istediğinize emin misiniz?
        <form asp-action="Delete" method="post">
            <input type="hidden" asp-asp-for="@Model.MusteriId">
            <button type="submit" class="btn btn-danger">Evet</button>
            <a asp-action="Index" class="btn btn-primary">Hayır</a>
        </form>
</p>